{
  "name": "opencv-processing-studio",
  "version": "1.0.0",
  "description": "A comprehensive desktop application for OpenCV image processing",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development electron .",
    "build": "electron-builder",
    "setup-python": "cd backend && python -m pip install -r requirements.txt"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.0.0"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "form-data": "^4.0.0"
  }
}
